<style>
  .carousel-caption {
  margin-bottom: 400px;
  }
</style>
<!-- header-section-ends -->
<div class="header-slider">
   <div style="height: 305px">
    <carousel interval="3000" no-wrap="noWrapSlides">
      <slide ng-repeat="post in posts track by $index" active="post.active">
        <img style="width: 100%; height: 800px;" src="{{post.cover}}" style="margin:auto;">
         <div class="carousel-caption">
          <div class="col-md-8 col-lg-8 col-xs-10 col-xs-offset-1 banner-inner">
            <h3>{{post.title}}</h3>
            <div class="banner_div">
              <p class="col-lg-4 col-md-4 col-sm-7 hidden-sm hidden-xs">&nbsp;</p>
            </div>
            <h3>{{post.tagline}}</h3>
            <div class="clearfix"></div>
            <p>{{post.description.substring(0, 30)}}</p>
            <p>&nbsp;</p>
              <a class="morebtn hvr-sweep-to-top" ui-sref="main.post({id:post.id})">VIEW THIS STORY</a>
          </div>
        </div>
      </slide>
    </carousel>
  </div>
</div>
<!----------->
<div class="content" style="margin-top: 600px;">
  <!---->
  <div class="container">
    <div class="top-grid-top">
      <div class="top-grids">
        <div class="col-md-4 col-sm-12 col-lg-4" ng-repeat="post in posts track by $index">
          <div class=" top-grid" >
            <a  ui-sref="main.post({id:post.id})"><img class="img-responsive" src="{{post.cover}}" title="name" alt=""></a>
               <a  ui-sref="main.post({id:post.id})"><h3>{{post.title}}</h3>
            <p>{{post.description.substring(0, 60)}}</p>
          </div>
        </div>
        <div class="clearfix"> </div>
      </div>
    </div>
         <div style="padding-top:30px; padding-bottom:30px; text-align:center;"><a href="#" class="morebtn hvr-sweep-to-top">LOAD MORE </a></div>
  </div>
</div>




