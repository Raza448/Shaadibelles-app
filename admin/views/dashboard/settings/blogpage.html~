 <div class="col-lg-6">


    <div class="form-group col-lg-12">
      <label>Small widgets</label>
      <select
        data-placeholder="Select a widgets"
        class="form-control"
        chosen
        multiple
        ng-model="settings.blog.smallWidgets"
        ng-options="item.id as item.title for item in widgets">
      </select>
    </div>


<div class="form-group col-lg-12">
      <label>Medium widgets</label>
      <select
        data-placeholder="Select a widgets"
        class="form-control"
        chosen
        multiple
        ng-model="settings.blog.mediumWidgets"
        ng-options="item.id as item.title for item in widgets">
      </select>
    </div>



<div class="form-group col-lg-12">
      <label>Big widgets</label>
      <select
        data-placeholder="Select widgets"
        class="form-control"
        chosen
        multiple
        ng-model="settings.blog.bigWidgets"
        ng-options="item.id as item.title for item in widgets">
      </select>
    </div>

<div class="form-group col-lg-12">
      <label>Slider</label>
      <select
        data-placeholder="Select widgets"
        class="form-control"
        chosen
        multiple
        ng-model="settings.blog.bigWidgets"
        ng-options="item.id as item.name for item in sliders">
      </select>
    </div>

<div class="form-group col-lg-12">
      <label>Recommended Categories</label>
      <select
        data-placeholder="Select categories"
        class="form-control"
        chosen
        multiple
        ng-model="settings.blog.categories"
        ng-options="item.id as item.name for item in categories">
      </select>
    </div>
  </div>
  <div class="col-lg-12">
    <a class="btn btn-lg btn-primary" ng-click="save(settings)">{{saveTxt}}</a>
  </div>
