<div>
    <div class="row" ng-controller="DashboardCtrl">
        <div class="col-lg-12">
            <h1 class="page-header">Dashboard</h1>
        </div>
        <!-- /.col-lg-12 -->
        <div class="col-lg-12">
           <a href="#/dashboard/users" ><div class="col-lg-3" >
              <i class="fa fa-user fa-5x"></i>
               <p>{{users.length}} users</p>
           </div></a>
           <a href="#/dashboard/vendors" > <div class="col-lg-3" >
              <i class="fa fa-suitcase fa-5x"></i>
               <p>{{vendors.length}} vendors</p>
           </div></a>
    
<a href="#/dashboard/posts" > <div class="col-lg-3" >
              <i class="fa fa-clipboard fa-5x"></i>
               <p>{{posts.length}} posts</p>
           </div></a>   


<a href="#/dashboard/weddings" > <div class="col-lg-3" >
              <i class="fa fa-birthday-cake fa-5x"></i>
               <p>{{weddings.length}} weddings</p>
           </div></a> 
<a href="#/dashboard/galleries" > <div class="col-lg-3" >
              <i class="fa fa-photo fa-5x"></i>
               <p>{{galleries.length}} galleries</p>
           </div></a> 
<a href="#/dashboard/reviews" > <div class="col-lg-3"  >
              <i class="fa fa-pencil-square-o fa-5x"></i>
               <p>{{reviews.length}} Reviews</p>
           </div></a> 

<a href="#/dashboard/ratings" > <div class="col-lg-3" >
              <i class="fa fa-star-half-o fa-5x"></i>
               <p>{{ratings.length}} Ratings</p>
           </div></a> 
      
        </div>
        <div class="col-lg-6">
 <table class="ibox-content table table-hover">
              <!-- shortcuts table header-->
              <thead>
                <tr>
                 


                  <th>Reviews</th>
                  <th>Visible</th>
                  <th></th>
                </tr>
              </thead>
              <!--shortcuts table header ends here-->
              <!-- shortcuts table data-->
              <tbody>
                <tr ng-repeat="item in reviews | where : { 'active' : false } |  filter: searchText">
                 
                  </td>
                  <td>{{item.content.substring(0, 100)}}
                  </td>
                  <td>
                    <a class="btn btn-sm btn-success" ng-show="item.active" ng-click="disable(item)">Disable</a>
                    <a class="btn btn-sm btn-warning" ng-hide="item.active" ng-click="activate(item)">Enable</a>
                  </td>
                  <td>
                    <a class="btn btn-sm btn-primary" href="#/dashboard/edit-review?id={{item.id}}">Edit</a>
                  </td>
                </tr>
              </tbody>
              <!-- shortcuts table data ends here-->
            </table>
</div>

  <div class="col-lg-6">
 <table class="ibox-content table table-hover">
              <!-- shortcuts table header-->
              <thead>
                <tr>
                 


                  <th>Reviews</th>
                  <th>Visible</th>
                  <th></th>
                </tr>
              </thead>
              <!--shortcuts table header ends here-->
              <!-- shortcuts table data-->
              <tbody>
                <tr ng-repeat="item in queries |  filter: searchText">
                 
                  </td>
                  <td>{{item.content.substring(0, 100)}}
                  </td>
                  <td>
                    <a class="btn btn-sm btn-success" ng-show="item.active" ng-click="disable(item)">Disable</a>
                    <a class="btn btn-sm btn-warning" ng-hide="item.active" ng-click="activate(item)">Enable</a>
                  </td>
                  <td>
                    <a class="btn btn-sm btn-primary" href="#/dashboard/edit-review?id={{item.id}}">Edit</a>
                  </td>
                </tr>
              </tbody>
              <!-- shortcuts table data ends here-->
            </table>
</div>
    </div>

    <!-- /.row -->
</div>
